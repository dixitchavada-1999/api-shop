# Environment Configuration Example
# Copy this file to .env and update the values

# Environment
# Set to 'production' when deploying to Railway/Render
NODE_ENV=development

# Server Port
# Railway automatically sets PORT, but you can override it
PORT=3000

# MongoDB Connection Strings
# Use LOCAL for development on your machine
MONGO_URI_LOCAL=mongodb://127.0.0.1:27017/api-shop

# Use DEV for development cloud database
MONGO_URI_DEV=mongodb+srv://dixitchavada1999_db_user:inh6c6OcwnPkOfM3@dev-cluster.521ewe8.mongodb.net/?appName=dev-cluster

# Default MongoDB URI (uncomment the one you want to use)
# For Railway: Set this in Railway Environment Variables to your MongoDB Atlas connection string
MONGO_URI=mongodb://127.0.0.1:27017/api-shop
# MONGO_URI=mongodb+srv://dixitchavada1999_db_user:inh6c6OcwnPkOfM3@dev-cluster.521ewe8.mongodb.net/?appName=dev-cluster

# JWT Secret (CHANGE THIS IN PRODUCTION!)
# Generate a strong random string for production
# Example: openssl rand -base64 32
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production_12345

# JWT Expiry
JWT_EXPIRE=30d

# Cloudinary Configuration (REQUIRED for production/Railway)
# Get your credentials from: https://cloudinary.com/console
# For Railway: Set these in Railway Environment Variables
CLOUDINARY_CLOUD_NAME=your_cloud_name
CLOUDINARY_API_KEY=your_api_key
CLOUDINARY_API_SECRET=your_api_secret

# CORS Configuration (Optional)
# For production, specify allowed origins separated by commas
# Example: ALLOWED_ORIGINS=https://yourapp.com,https://www.yourapp.com
# Leave empty or set to * to allow all origins (not recommended for production)
ALLOWED_ORIGINS=*

